<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "RoRcipes" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
      <div class="container-fluid">
        <%= link_to root_path, class: "navbar-brand fw-bold d-flex align-items-center" do %>
          <i class="bi bi-trophy-fill me-2"></i>
          <span>ChallengeHub</span>
        <% end %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="mainNav">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to categories_path, class: "nav-link" do %>
                <i class="bi bi-grid-3x3-gap me-1"></i> Categories
              <% end %>
            </li>
            <li class="nav-item">
              <%= link_to challenges_path, class: "nav-link" do %>
                <i class="bi bi-flag me-1"></i> Challenges
              <% end %>
            </li>
            
            <% if user_signed_in? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="activityDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  <i class="bi bi-activity me-1"></i> My Activity
                </a>
                <ul class="dropdown-menu" aria-labelledby="activityDropdown">
                  <li>
                    <%= link_to bookmarked_challenges_path, class: "dropdown-item" do %>
                      <i class="bi bi-bookmark-fill me-2"></i> Bookmarks
                    <% end %>
                  </li>
                  <li>
                    <%= link_to challenge_participations_path, class: "dropdown-item" do %>
                      <i class="bi bi-people-fill me-2"></i> My Participations
                    <% end %>
                  </li>
                  <li>
                    <%= link_to progress_logs_path, class: "dropdown-item" do %>
                      <i class="bi bi-graph-up me-2"></i> Progress Logs
                    <% end %>
                  </li>
                  <li><hr class="dropdown-divider"></li>
                  <li>
                    <%= link_to badges_path, class: "dropdown-item" do %>
                      <i class="bi bi-award-fill me-2 text-warning"></i> My Badges
                    <% end %>
                  </li>
                </ul>
              </li>
              
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="interactionsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  <i class="bi bi-envelope me-1"></i> Interactions
                </a>
                <ul class="dropdown-menu" aria-labelledby="interactionsDropdown">
                  <li>
                    <%= link_to challenge_requests_path, class: "dropdown-item" do %>
                      <i class="bi bi-box-arrow-in-right me-2"></i> Join Requests
                    <% end %>
                  </li>
                  <li>
                    <%= link_to challenge_invites_path, class: "dropdown-item" do %>
                      <i class="bi bi-send-fill me-2"></i> Invitations
                    <% end %>
                  </li>
                </ul>
              </li>
              
              <li class="nav-item">
                <% unread = current_user.notifications.where(is_read: false).count %>
                <%= link_to notifications_path, class: "nav-link position-relative" do %>
                  <i class="bi bi-bell-fill me-1"></i> Notifications
                  <% if unread.positive? %>
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                      <%= unread > 99 ? '99+' : unread %>
                      <span class="visually-hidden">unread notifications</span>
                    </span>
                  <% end %>
                <% end %>
              </li>
              
              <% if can? :manage, User %>
                <li class="nav-item">
                  <%= link_to users_path, class: "nav-link" do %>
                    <i class="bi bi-shield-fill-check me-1"></i> Admin
                  <% end %>
                </li>
              <% end %>
            <% end %>
          </ul>
          
          <div class="d-flex align-items-center gap-2">
            <% if user_signed_in? %>
              <div class="dropdown">
                <a class="btn btn-outline-light btn-sm dropdown-toggle d-flex align-items-center gap-2" href="#" role="button" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                  <% if current_user.avatar_url.present? %>
                    <img src="<%= current_user.avatar_url %>" alt="Avatar" class="rounded-circle" width="24" height="24">
                  <% else %>
                    <i class="bi bi-person-circle"></i>
                  <% end %>
                  <span><%= current_user.username || current_user.email&.split('@')&.first %></span>
                  <span class="badge bg-light text-primary"><%= current_user.role %></span>
                </a>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                  <li>
                    <h6 class="dropdown-header">
                      <%= current_user.email %>
                    </h6>
                  </li>
                  <li><hr class="dropdown-divider"></li>
                  <li>
                    <%= link_to edit_user_registration_path, class: "dropdown-item" do %>
                      <i class="bi bi-gear me-2"></i> Edit Profile
                    <% end %>
                  </li>
                  <li><hr class="dropdown-divider"></li>
                  <li>
                    <%= button_to destroy_user_session_path, method: :delete, class: "dropdown-item text-danger" do %>
                      <i class="bi bi-box-arrow-right me-2"></i> Log Out
                    <% end %>
                  </li>
                </ul>
              </div>
            <% else %>
              <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-light btn-sm" %>
              <%= link_to "Log In", new_user_session_path, class: "btn btn-outline-light btn-sm" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

<style>
  .navbar {
    border-bottom: 3px solid rgba(255, 255, 255, 0.1);
  }
  
  .navbar-brand {
    font-size: 1.5rem;
    letter-spacing: -0.5px;
  }
  
  .nav-link {
    transition: all 0.2s ease;
    border-radius: 0.25rem;
    margin: 0 0.125rem;
  }
  
  .nav-link:hover {
    background-color: rgba(255, 255, 255, 0.1);
    transform: translateY(-1px);
  }
  
  .dropdown-menu {
    border: none;
    box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
    border-radius: 0.5rem;
  }
  
  .dropdown-item {
    transition: all 0.2s ease;
  }
  
  .dropdown-item:hover {
    background-color: rgba(13, 110, 253, 0.1);
    transform: translateX(4px);
  }
</style>

    <div class="container mt-3">
      <% if notice %>
        <div class="alert alert-success fade show">
          <%= notice %>
          <button_type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close", aria-label="close"></button>   
        </div>
      <%end%>
    </div>
      
    <%= yield %>
  </body>
</html>
